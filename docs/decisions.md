# Décisions

Deux-cent-vingt-trois décisions actées, avec l'alternative écartée et sa raison.
Les détails d'application sont dans les documents thématiques.

| # | Décision | Alternative écartée et raison |
|---|---|---|
| D1 | Un dépôt et un VPS par client, socle commun partagé | Multi-tenant mutualisé : inutile à cette échelle, et supprime l'isolation qui répond à C3 |
| D2 | Contenu en fichiers, panel hébergé sur le VPS du client | CMS SaaS ou base de données : ajoute une surface d'attaque et rend le contenu invisible depuis le dépôt (C4) |
| D3 | Le client édite les contenus, réordonne et masque les sections ; il n'en ajoute pas et ne crée pas de pages | Liberté totale : impose de gérer URLs, navigation et sitemap, et fragilise la DA |
| D4 | Périmètre : formulaire de contact, analytics léger, multilingue. Pas de blog | — |
| D5 | Le socle est un package installé depuis git par tag, doublé d'un CLI | Template de dépôt : les améliorations ne redescendent jamais. Registre npm privé : infra inutile à cette échelle |
| D6 | Panel d'édition maison, formulaires générés depuis les schémas | Keystatic, Sveltia, Decap : aucun ne couvre « panel authentifié sur VPS écrivant sur disque », et l'authentification reste à écrire dans tous les cas |
| D7 | TypeScript partout ; Astro pour le rendu ; React + Mantine pour le panel ; CSS natif, pas de Tailwind | Tailwind : ferait un troisième paradigme de style à côté de Mantine, pour le même résultat par un chemin plus long |
| D8 | Chaînes d'interface non éditables ; contenu traduisible imbriqué dans les champs ; structure de page partagée entre langues | Un fichier par langue : la structure s'écrit deux fois, donc elle diverge |
| D9 | Authentification : email + mot de passe généré + code à usage unique par email ; appareil de confiance 30 jours | Passkeys seules : iCloud ne sort pas de l'écosystème Apple et seuls 29 % des utilisateurs vont au bout du rattrapage par QR code |
| D10 | Enregistrer et publier sont deux actions distinctes ; preview du brouillon | Publication automatique : le client ne peut pas préparer un chantier, et chaque état intermédiaire devient public |
| D11 | Publication par bascule atomique de lien symbolique entre versions datées | Écriture en place : un build interrompu casse un site en production |
| D12 | Toute image téléversée est ré-encodée ; SVG interdit à l'upload | Conserver le fichier reçu : laisse passer les fichiers polyglottes et les charges utiles en métadonnées |
| D13 | Email via Brevo par défaut, derrière une interface agnostique | Resend : société américaine, or les leads sont des données personnelles. Postmark : meilleure délivrabilité mais 100 emails/mois gratuits |
| D14 | Analytics par analyse des logs Caddy, rapport dans le panel | GoatCounter ou Umami : ajoutent un script sur les pages ou un runtime supplémentaire sur la machine |
| D15 | Déploiement par Docker Compose | Bare metal : plus léger, mais le provisionnement diverge d'un VPS à l'autre — le défaut même que ce socle corrige |
| D16 | Le socle est un dépôt **public** | Privé : chaque VPS aurait besoin d'une clé de lecture GitHub, donc un secret de plus à distribuer, pour une confidentialité qui ne fait pas partie du modèle de sécurité |
| D17 | Le panel commit à chaque enregistrement, mais ne pousse qu'à la publication | Pousser à chaque enregistrement : l'enregistrement dépend alors du réseau, et l'historique distant ne correspond plus à ce qui est en ligne |
| D18 | Une langue s'active d'abord en préparation (`draft`) ; elle n'exige ses traductions qu'une fois en ligne | Activation directe : tout le site devient invalide d'un coup et le client ne peut plus rien publier tant qu'il n'a pas tout traduit |
| D19 | Les blocs de base sont une référence technique — un bloc par mécanique du socle — pas un catalogue de sections | Catalogue : les variantes visuelles sont du sur-mesure client, elles n'enseignent rien et se maintiennent pour rien |
| D20 | Le mot de passe initial n'est jamais transmis par email | Envoi par email : les deux facteurs arrivent dans la même boîte, le second facteur ne protège plus de rien |
| D21 | Un commentaire décrit l'existant ; le pourquoi d'un choix va dans ce fichier | Commentaires d'historique : ils vieillissent, personne ne les supprime, et ils noient les rares commentaires utiles |
| D22 | Pas de `utils.ts` ni de dossier `helpers/` : un helper vit dans le dossier de son domaine | Fourre-tout : personne ne le lit, tout le monde y ajoute — c'est là que la duplication s'accumule |
| D23 | Une mise à jour est atomique : elle aboutit entièrement, ou le dépôt revient à l'état d'avant | Suite d'étapes manuelles : un site à moitié migré est le pire état possible |
| D24 | Caddy comme reverse proxy | nginx : certificats à gérer avec certbot et son renouvellement, configuration quatre fois plus longue, en-têtes de proxy faciles à oublier |
| D25 | Le panel emploie le vocabulaire du client (« section », « mettre en ligne »), jamais celui du code | Vocabulaire technique : le client doit apprendre un modèle mental qui n'est pas le sien pour utiliser son propre site |
| D26 | Configuration en deux fichiers : `site.config.ts` versionné, `.env` non versionné | Tout dans un seul fichier : soit les clés partent sur GitHub, soit la DA cesse d'être versionnée |
| D27 | La doc agent du dépôt client est régénérée à chaque `npm install` dans `.claude/basalte.md`, importée par un `CLAUDE.md` écrit à la main | Doc générée une seule fois à l'init : fausse dès la première montée de version. Doc laissée dans `node_modules` : un agent ne la lit pas de façon fiable |
| D28 | Un bloc ne contient aucune valeur de style en dur : tout passe par un token | Valeurs en dur : la DA n'est plus pilotable depuis `site.config.ts` et chaque bloc dérive |
| D29 | La mise en production est une commande idempotente lancée depuis ta machine | Guide de provisionnement : jamais à jour, et chaque VPS finit différent — le défaut que D15 corrige |
| D30 | `basalte doctor` prouve la configuration au lieu de la vérifier : email réellement envoyé, DNS réellement résolu | Contrôle de forme : une clé présente mais fausse passe, et se découvre le jour où le client ne peut plus se connecter |
| D31 | Trois niveaux d'engagement — invariant, décidé, hypothèse — et le *comment* d'une phase se décide dans la phase | Tout figer d'avance : des choix pris sans pouvoir les évaluer, suivis par discipline, payés en dette. Ne rien figer : les invariants se perdent |
| D32 | Un seul `package-lock.json` portant les binaires de `linux-x64` et de `win32-x64`, vérifié par `lockfile:check` ; image Docker en base glibc | Alpine et musl : une troisième famille de binaires à faire coexister dans le même lockfile, pour un gain de taille d'image que `deploiement.md` a déjà renoncé à chercher |
| D33 | Node 24 épinglé partout : `.nvmrc`, `engines`, `engine-strict`, image Docker | Version flottante : deux machines de développement qui divergent en silence, et un lockfile réécrit au gré de celle qui a lancé `npm install` |
| D34 | TypeScript 6, pas 7 | TypeScript 7 est `latest`, mais `@astrojs/check` déclare `typescript: ^5.0.0 \|\| ^6.0.0` — le typecheck des `.astro` casserait dès le premier bloc |
| D35 | Vitest pour les tests | `node:test` : zéro dépendance, mais aucune continuité avec l'écosystème Vite d'Astro le jour où un composant se teste |
| D36 | Prettier seul pour le formatage, aucun linter généraliste ; la documentation en est exclue | ESLint typé : les règles qui comptent ici — valeur de style en dur, duplication, registre central — ne s'expriment pas en règles de linter généraliste. Elles vont dans une commande du socle, `basalte lint` (D135) |
| D37 | Hooks git natifs par `core.hooksPath`, branchés par `npm run setup` | husky : s'installe par `prepare`, or `prepare` est déjà la compilation du package et s'exécute chez le client — le hook s'installerait dans le clone temporaire du consommateur |
| D38 | CI sur les pull requests uniquement, matrice ubuntu + windows, plus un job qui installe le package depuis git | Sur chaque push : la matrice Windows double la facture de minutes pour une information que la PR donne déjà |
| D39 | React 19.2 avec le compilateur React activé dès le départ ; `astro` en dépendance de pair à la version exacte | Compilateur branché plus tard : tout le `useMemo` écrit d'ici là devient du bruit à retirer, et le panel a été conçu autour. `astro` en dépendance ordinaire : deux copies possibles, donc deux instances de Vite et une intégration chargée deux fois, en silence |
| D40 | Les dérivées d'une image sont produites à l'**ingestion** par sharp et versionnées ; le build ne traite aucune image | Optimisation au build par Astro : `import.meta.glob` à rebrancher, un cache d'images à préserver entre publications, et le pic mémoire de sharp sur un VPS de 2 Go à chaque mise en ligne. Le ré-encodage étant déjà obligatoire au téléversement (invariant 3), produire les largeurs dans la même passe ne coûte rien |
| D41 | Un champ traduisible porte toujours sa carte de langues, même sur un site monolingue | Valeur nue tant qu'il n'y a qu'une langue : deux formes à lire partout, et activer une deuxième langue imposerait de réécrire tout le contenu — le coût que D18 cherchait à supprimer |
| D42 | Le Markdown restreint est rendu par le socle, en échappant d'abord puis en réintroduisant une liste blanche | `markdown-it` plus un assainisseur : deux dépendances de plus sur chaque VPS, et une surface bien plus large que le gras, l'italique et les liens |
| D43 | Un champ absent d'un contenu vaut vide ; le caractère requis se dit par sa borne basse | Exiger la présence de chaque clé : un JSON écrit à la main devient pénible, le message parle d'absence là où le client comprend « à remplir », et le composant doit tester deux cas |
| D44 | Les routes des langues sont produites par `getStaticPaths` | Configuration `i18n` d'Astro : elle apporte des redirections, des replis et un `currentLocale` dont rien n'a besoin ici, et il faudrait quand même écarter les langues en préparation à la main |
| D45 | Ce que le rendu consomme est un vrai fichier, écrit dans le dossier de génération du projet | Module purement virtuel : la collecte des styles d'Astro parcourt le graphe des modules et ne le traverse pas — les blocs perdent leur CSS, en silence et sans erreur |
| D46 | Le site de démonstration n'est ni un paquet npm ni un workspace : il se résout par self-reference | Workspace npm : la seule présence du champ `workspaces` déclenche une installation complète dans le clone temporaire, chez chaque client, à chaque `npm ci` (`environnement.md`) |
| D47 | La base est `node:sqlite`, le module intégré à Node 24 | `better-sqlite3` : une compilation native de plus sur chaque VPS et une famille de binaires de plus à faire coexister dans le lockfile (D32), pour une API équivalente |
| D48 | Hachage Argon2id par `@node-rs/argon2`, aux paramètres OWASP — 19 Mio, deux passes, un fil | `argon2` : son `install` compile sur place quand aucun binaire préconstruit ne correspond, sur un VPS qui n'a pas de chaîne de compilation. `hash-wasm` : aucun binaire, mais une couche WASM là où le lockfile sait déjà porter des binaires par plateforme |
| D49 | Le code à six chiffres est haché **avec le jeton de la tentative**, qui ne vit que dans le navigateur | Hacher le code seul : vingt bits d'entropie, qu'une base volée retrouve en une seconde. Le jeton absent de la base, l'empreinte ne se retourne plus |
| D50 | Limitation de débit par compteur en fenêtre fixe, une ligne par fenêtre | Fenêtre glissante : une ligne par tentative, pour une précision dont rien ici n'a l'usage — l'objectif est de ramener un million d'essais à quelques dizaines |
| D51 | Le flux d'authentification s'expose en fonctions `Request` vers `Response`, sans rien savoir du serveur qui les monte | Des routes Astro directement : le flux ne serait testable qu'à travers un serveur, et le panel de la phase 3 aurait à le réécrire pour l'éprouver |
| D52 | Deux gardes indépendantes contre le CSRF : corps annoncé en JSON, et `Origin` du même hôte que la requête | Jeton anti-CSRF synchronisé : un état de plus à stocker et à faire circuler dans chaque formulaire, pour une propriété que `SameSite=Strict` et ces deux gardes tiennent déjà |
| D53 | `basalte admin:login --create` crée le compte et affiche son mot de passe | Une dixième commande : la surface du CLI est un engagement (`depot-client.md`), et la création d'un compte est déjà une opération de console, au même endroit et avec les mêmes droits |
| D54 | Les phases suivent leur numérotation : la 2 vient après la 1 | Avancer la phase 6 : un vrai site en ligne bien plus tôt, au prix d'un aller-retour sur `init` quand le panel arrivera |
| D55 | Le panel et le site public sortent du même projet Astro, en deux modes : `astro build` construit le site, statique ; `BASALTE_MODE=panel astro build` construit le panel, en sortie serveur derrière l'adaptateur Node | Un seul build hybride : le processus qui publie se reconstruirait lui-même, et devrait redémarrer pour annoncer son propre succès. Un second projet Astro : les blocs sur mesure du dépôt client seraient à rebrancher, et l'aperçu cesserait d'employer les composants réels |
| D56 | Le panel reçoit les schémas dans le module généré au démarrage ; il ne parcourt plus les blocs à l'exécution | Scanner à chaque requête : une fois le serveur du panel groupé, `import.meta.url` ne désigne plus le dossier des blocs du socle, et seuls ceux du dépôt client sont trouvés — une panne silencieuse, rencontrée en phase 3. Les descripteurs étant des données pures, les sérialiser ne coûte rien |
| D57 | Aucune dépendance nouvelle pour le panel : `@mantine/core`, `@mantine/hooks` et dnd-kit suffisent | `@mantine/form`, `@mantine/notifications`, `@mantine/modals`, `@mantine/dropzone` : quatre paquets de plus sur chaque VPS (`environnement.md`) pour des composants que `@mantine/core` porte déjà |
| D58 | L'état d'édition est le document entier, recomposé de bas en haut : chaque champ rend une nouvelle valeur à son parent | `@mantine/form` : sa validation ferait une deuxième source de vérité à côté des schémas, et ses aides de liste supposent une forme connue du type — or la forme vient des descripteurs, à l'exécution |
| D59 | Un composant de panel par type de champ, choisi par une table unique `kind → composant` | Un formulaire qui traite les types un par un dans chaque écran : chaque type ajouté au DSL coûterait une modification du panel, et le levier de la phase 1 serait perdu |
| D60 | Un enregistrement invalide est refusé, avec les messages français de `check` | Enregistrer quand même : l'enregistrement produit un commit (D17), et un commit qui ne se construit pas ferait échouer la mise en ligne suivante sans que le client sache pourquoi |
| D61 | Le panel écrit ce que la validation a produit, donc toutes les clés déclarées | Réécrire ce que le navigateur a envoyé : des clés inconnues s'accumuleraient dans le contenu, et le diff d'un enregistrement dépendrait de ce que le panel avait chargé |
| D62 | Le panel ne commite que si la racine du site est la racine d'un dépôt git, et il commite sans les hooks | Commiter dès qu'un dépôt existe au-dessus : le panel écrirait dans l'historique d'un autre dépôt — le site de démonstration, logé dans celui du socle, le prouve. Laisser tourner les hooks : `check` vient de passer, et un hook en échec rendrait l'enregistrement impossible |
| D63 | Cinq pages dans le panel : « Réglages » n'existe pas | La page « Réglages » de l'hypothèse de départ : langues et informations du site vivent dans `site.config.ts`, versionné, que le client n'édite pas (D26). Une page vide est une page de trop |
| D64 | Le panel sert `/media/*` depuis le dépôt, jamais depuis la version construite | Servir les fichiers de la version en ligne : une image téléversée resterait invisible dans le panel et dans l'aperçu jusqu'à la mise en ligne suivante |
| D65 | Le panel emploie l'échelle de Mantine, pas les tokens du site | Étendre au panel la règle des tokens : la DA d'un client déciderait alors de la lisibilité de son outil de travail, et deux directions artistiques se disputeraient le même écran |
| D66 | Un enregistrement est acquis dès que le fichier est écrit : un git en panne le prive de son point de retour, jamais de son contenu | Faire échouer l'enregistrement avec le commit : le fichier est déjà sur le disque, et le client verrait une erreur devant un travail pourtant sauvé. L'échec part sur la sortie d'erreur, comme celui d'un envoi d'email |
| D67 | Le build d'une mise en ligne tourne en processus enfant, plafonné en mémoire et dans le temps | L'API JavaScript d'Astro dans le processus du panel : un build qui sature la mémoire ou qui ne revient jamais emporterait avec lui le seul écran capable de le relancer |
| D68 | Le build écrit directement dans le dossier de la version, encore inachevé, jamais dans `dist/` | Construire dans `dist/` puis déplacer : `dist/` porte le panel construit — celui que le processus en cours exécute — et un déplacement entre deux volumes n'est plus un renommage |
| D69 | La racine servie vient de `BASALTE_SITE_ROOT` : `/srv/site` en production, `.basalte/site` dans le dépôt sinon | Un chemin en dur : le socle se développe et se teste hors d'un conteneur, et deux sites sur une même machine de développement se marcheraient dessus |
| D70 | La bascule écrit le lien à côté puis le renomme par-dessus | Supprimer puis recréer : une requête tombant dans l'intervalle ne trouve rien. Windows ne sait pas renommer par-dessus une jonction et retombe sur ce chemin-là — c'est une machine de développement, la production est sous Linux |
| D71 | La file d'attente n'a qu'une place, et une nouvelle demande remplace celle qui attend | Empiler les demandes : deux builds Astro simultanés saturent la mémoire d'un petit VPS, et trois clics d'un client impatient ne valent qu'une seule mise en ligne |
| D72 | L'ordre est : rebaser, construire, basculer, pousser | Construire avant de rebaser : le conflit avec tes propres commits est le seul échec vraiment probable, et il doit coûter zéro seconde de build. Pousser avant de basculer : le site ne sortirait plus quand GitHub est indisponible |
| D73 | Les mises en ligne vivent dans la base, en états terminaux seulement | Une ligne « en cours » : elle survivrait à un redémarrage et mentirait pour toujours. Un fichier d'état à côté des versions : une deuxième source de vérité à sauvegarder |
| D74 | Le rebase et le push sont gardés par la même règle que les commits : la racine du site doit être la racine du dépôt | S'en remettre à git : il répond pour le dépôt le plus proche au-dessus, et publier un dossier logé dans un autre dépôt — le site de démonstration — rebaserait et pousserait celui-là (D62) |
| D75 | L'alerte au mainteneur emprunte le canal du site (`EMAIL_*`), jamais celui des codes de connexion | Le canal d'authentification : une machine qui échoue en boucle épuiserait le quota qui sert à se connecter, et enfermerait dehors la seule personne capable de réparer |
| D76 | Le formulaire de contact est un formulaire HTML ordinaire : POST classique, réponse en redirection, et la page révèle le message par `:target` | Un envoi par `fetch` : le site public embarquerait du JavaScript (invariant 5), et un lead se perdrait le jour où le script ne charge pas. Une page de remerciement rendue par le serveur : elle ne porterait pas la DA du client, versionnée dans son dépôt |
| D77 | L'anti-spam est un champ leurre, une limitation par adresse et un plafond pour le site ; le délai minimum de remplissage est abandonné | Le délai de `services.md` : il demande une horloge posée au chargement, donc du JavaScript. Un CAPTCHA : un tiers dans la page, un obstacle d'accessibilité, et des visiteurs perdus pour arrêter ce que le leurre arrête déjà |
| D78 | Un leurre rempli reçoit la réponse d'un envoi réussi, et rien n'est écrit ; une limite atteinte, elle, se dit franchement | Refuser le leurre visiblement : le robot apprend quel champ le trahit et le contourne au prochain passage. Mentir sur la limite : un visiteur réel croirait son message parti |
| D79 | L'adresse de retour est reconstruite depuis les pages du dépôt, jamais recopiée de ce que le formulaire a envoyé | Rediriger vers le champ reçu : c'est une redirection ouverte, offerte à qui poste le formulaire depuis n'importe où |
| D80 | Un message est écrit en base avant tout envoi, et la ligne dit si la notification est partie | Envoyer puis écrire : un incident chez le fournisseur perdrait le lead, qui coûte plus cher que le reste du site réuni. Un état « en cours » : il survivrait à un redémarrage et mentirait pour toujours (D73) |
| D81 | Le destinataire des messages est `CONTACT_EMAIL` dans `.env`, à côté de `EMAIL_ADMIN` | Dans `site.config.ts` : l'adresse du client partirait sur GitHub avec le dépôt, et les deux destinataires de la machine se liraient dans deux fichiers différents |
| D82 | Les libellés du formulaire sont des champs, vides par défaut, et le composant retombe alors sur le français | Une table de traductions dans le socle : une langue non prévue demanderait de modifier le socle pour un texte qui appartient au client. Des libellés en dur : un site anglais afficherait « Envoyer » |
| D83 | C'est le processus du panel qui purge, au démarrage puis chaque jour ; les logs d'accès, eux, tournent avec `roll_keep_for` | Un cron dans le conteneur : un composant de plus à provisionner et à surveiller, pour trois `delete` que le processus déjà en place exécute en une milliseconde |
| D84 | L'audience se lit dans le fichier de log courant, par la fin et sur huit méga-octets au plus | Lire aussi les fichiers tournés : ils sont compressés par Caddy, et les décompresser pour un rapport d'appoint mettrait la mémoire du panel à la merci d'un log volumineux |
| D85 | Le panel range ses fichiers dans `_panel/`, le site public garde `_astro/` | Un dossier commun : le proxy sert le site depuis le disque et le panel depuis l'application, et l'island du panel serait cherchée parmi les fichiers du site — une page vide, sans la moindre erreur côté serveur |
| D86 | Une traduction dont la langue n'est plus déclarée est conservée dans le fichier de contenu, pas retirée | L'écarter à l'analyse : le panel réécrit ce que l'analyse rend, si bien que retirer une langue de `site.config.ts` effacerait tout son texte au premier enregistrement — une perte silencieuse, que seul l'historique git rattrape et que le client ne saura pas y chercher |
| D87 | Les migrations de format vivent sous `src/migrations/`, en liste ordonnée écrite à la main | Un dossier `migrations/` à la racine : `tsconfig.build.json` ne compile que `src/`, et Node refuse d'effacer les types sous `node_modules` — la migration n'arriverait jamais exécutable chez le client. Un dossier parcouru : l'ordre d'une suite de migrations serait confié à un tri de noms de fichiers |
| D88 | La mise en ligne se déclenche au démarrage du processus quand la version servie ne vient pas du commit courant ; une middleware ouvre le panel à la première requête, quelle qu'elle soit, sauf sous `astro dev` | Une dixième commande `basalte publish` lancée par `deploy` : son build tournerait hors de la file à une place (D71), et la surface du CLI est un engagement (D53). Un endpoint appelé par `deploy` : un secret de machine de plus à distribuer. Laisser le contexte s'ouvrir à la demande : seules quelques routes l'ouvraient, et une machine pouvait servir `/admin` sans lancer ni la purge ni la publication. Laisser la règle valoir aussi en développement : aucune version n'y est jamais servie, si bien qu'ouvrir une page lancerait un build de production sur un poste qui ne met rien en ligne |
| D89 | `.claude/basalte.md` est écrit par `basalte inventory --agent`, appelé par le `postinstall` du dépôt client | Une dixième commande : le contenu du fichier *est* l'inventaire mis en forme pour un agent, et le CLI reste à neuf commandes |
| D90 | Les notes de version vivent dans `notes/vX.Y.Z.md`, livrées dans le paquet et lues sur le dépôt public | Le message du tag annoté : `git ls-remote` ne rend pas les messages, il faudrait un `fetch` du socle depuis le dépôt client ou un jeton pour un dépôt pourtant public |
| D91 | La clé de déploiement est engendrée sur la machine par `deploy`, et sa moitié publique enregistrée sur le dépôt | La produire sur la machine du mainteneur, à l'`init` : la moitié privée devrait alors voyager, et un dépôt créé à la main n'aurait jamais de clé |
| D92 | Le dépôt du site est monté dans le conteneur ; l'installation et le build du panel ont lieu au démarrage, sautés tant que le verrou de dépendances n'a pas changé | Copier le dépôt dans l'image : le panel écrit du contenu, commite et pousse, et ces écritures seraient perdues à chaque redémarrage — ou invisibles du `git pull` de `deploy` |
| D93 | `doctor` envoie un vrai email ; `memoryProvider` ne sert qu'aux tests | Prouver le rendu en mémoire, comme `services.md` le laissait entendre : une clé présente mais fausse passerait, et se découvrirait le jour où le client ne peut plus se connecter (D30) |
| D94 | `update` exige un arbre de travail propre avant de commencer | Ranger automatiquement ce qui traîne : un rangement qui échoue laisse le dépôt dans un état que la commande ne sait plus défaire, or son unique promesse est de pouvoir tout annuler (D23) |
| D95 | Le panel porte sa propre couche de tokens (`src/admin/theme.ts`), injectée dans le thème Mantine et posée en variables `--panel-*` | Subir l'échelle par défaut de Mantine (D65 lue au pied de la lettre) : la DA se serait alors écrite composant par composant, et chaque écran aurait dérivé de son côté — le défaut que la règle des tokens interdit aux blocs |
| D96 | L'aperçu est le centre de l'écran d'édition, entre la liste des sections et le panneau de la section choisie ; il montre le dernier enregistrement | Le bouton « Aperçu » qui ouvrait un onglet : le client écrivait sans voir, et vérifiait après coup. Montrer le brouillon non enregistré demanderait une route de rendu qui accepte un contenu en transit — c'est la phase suivante, pas un préalable |
| D97 | Le panel ne dessine aucune bordure : un écart de valeur et une ombre très douce séparent deux plans | Le trait de 1 px de Mantine : sur un écran qui empile listes, cartes et panneaux, les traits s'accumulent et durcissent l'outil que le client ouvre chaque semaine |
| D98 | Un site déclare ce qu'il fait dans `capabilities` de `site.config.ts`, lu à l'exécution ; `init` n'écrit jamais deux socles différents | Des drapeaux d'`init` qui bifurqueraient la génération : trois interrupteurs feraient huit variantes à maintenir, et la promesse de D5 — un correctif atteint un site en changeant un numéro — ne tiendrait plus |
| D99 | La grammaire enrichie de `f.richtext` — titres et listes — est déclarée champ par champ, par `headings` et `lists` | Un neuvième type `f.prose()` : un type de plus dans le DSL, le Zod, la description et le panel pour la même mécanique. L'étendre partout : un `##` deviendrait possible dans un corps de section qui porte déjà son titre |
| D100 | Un document légal est une page de contenu ordinaire portant une section `richtext`, pré-remplie par `init` d'un canevas à compléter | Un type de page ou un bloc dédié : D19 refuse un bloc qui est un `richtext` habillé autrement, et une page à part demanderait une exception dans `getStaticPaths`, la liste du panel et le sitemap |
| D101 | Les faits de l'entreprise n'ont pas de source structurée : ils vivent dans le contenu que le client édite | Un `business.ts` typé : les faits n'ont qu'un consommateur — la page qui les affiche — que le client gère déjà, et une source de plus ouvrirait un sixième écran de panel que D63 refuse |
| D102 | Le PDF est accepté en téléchargement seul, sous six conditions écrites dans `securite.md` — seule exception à l'invariant 3 | Le refus pur : un client qui a des conditions générales signées ne peut pas les retranscrire. L'accepter sans condition : un PDF est un format à script, et rien ne le ré-encode |
| D103 | Les deux rendus sortent d'**un seul build** et vivent dans la même version, le bureau rangé sous `_desktop/` | Deux builds dans deux dossiers : le temps double pour de bon et le plafond de D67 est à réviser, `_astro/` est dupliqué, et la bascule porte sur deux dossiers là où l'atomicité (invariant 11) n'en veut qu'un. Un troisième rendu : hors périmètre, une tablette tombe d'un côté |
| D104 | Le composant d'un bloc **est** le rendu mobile ; le bureau est une variante facultative `<Nom>.desktop.astro`, avec repli sur le composant | Deux variantes symétriques `.mobile` / `.desktop` : un bloc écrit avant la phase perdrait son rendu par défaut, et le contrat SEO n'aurait plus de référence. Un `support` passé en prop au composant unique : le HTML replierait dans un seul fichier, et les deux CSS partiraient sur les deux supports |
| D105 | L'aiguillage est fait par **Caddy**, sur `Sec-CH-UA-Mobile` puis, à défaut, le User-Agent ; tout ce qui ne se déclare pas mobile — tablette comprise — reçoit le bureau | L'aiguillage dans le processus Node : le site public cesserait d'être servi indépendamment du panel, et couper l'édition couperait les visites (`architecture.md`). Le seul User-Agent : les navigateurs figent leur chaîne, et l'indication client arrive déjà à la première requête, étant à faible entropie |
| D106 | Le Caddyfile **ne bifurque pas** selon les capacités : la réécriture ne s'applique que si la page bureau existe, et retombe sinon sur le rendu mobile | Un Caddyfile conditionnel : il n'est écrit qu'à l'`init` et n'est pas régénéré par `update`, si bien qu'activer un second rendu après coup demanderait une intervention sur la machine — exactement l'irréversible que D98 interdit. `Vary` y est donc posé même sur un site à un seul rendu, ce qui ne coûte rien tant qu'aucun CDN n'est devant |
| D107 | `hidden` ne gagne **pas** d'axe de support : une section se masque par langue, jamais par support | Masquer par support : le mobile cesserait de porter tout le contenu, ou le bureau serait amputé sans que le panel sache le dire. Ce qui ne doit pas paraître au bureau est une affaire de présentation, donc de variante |
| D108 | Le contrat des deux rendus est vérifié sur le HTML produit, et il **avertit sans bloquer** | Refuser la mise en ligne : un mot décoratif ajouté au bureau ferait tomber un site qui fonctionne, alors que la conséquence se compte en référencement, pas en panne. Ne rien vérifier : chacun des deux rendus est correct isolément, le défaut est invisible à l'œil et se découvre des mois plus tard |
| D109 | Le chrome vit dans `src/chrome/<emplacement>/`, découvert comme un bloc, et **le site l’emporte sur le socle** | Un doublon en erreur comme pour les blocs : le chrome ne peut pas manquer, et un site qui redessine son en-tête devrait renommer ce que le socle porte — donc en perdre le repli. Un bloc réservé dans `src/blocks/` : il entrerait dans la bibliothèque des sections, où le client pourrait l’ajouter à une page |
| D110 | Le chrome est un manifeste de `content/` — `chrome.json` —, jamais une page | Un fichier de contenu ordinaire : une exception dans `getStaticPaths`, une dans la liste du panel, une dans le sitemap à venir — trois endroits où une exception s’oublie. Le mécanisme existait déjà pour `media.json` et `documents.json` |
| D111 | `chrome.json` porte un `$format`, et les migrations le traversent par un `chrome` optionnel | Pas de `$format` : le fichier porte du contenu validé contre des schémas, il dérivera comme une page. Un `$format` que `migrate` ignore : un numéro qui ne bouge jamais, plus trompeur que son absence |
| D112 | Les liens du menu sont une liste éditable, pré-remplie par `init` ; vide, elle retombe sur les pages du site, nommées par leur fichier | Une liste obligatoire : un site plus ancien que la phase se retrouverait sans menu à sa montée de version, sans qu’aucune migration puisse créer un fichier. Les pages seules : le client ne pourrait ni renommer, ni réordonner, ni renvoyer une page légale au pied (D3). Le `meta.title` comme libellé : c’est une phrase de résultat de recherche, pas une étiquette de menu |
| D113 | Le menu mobile est un `<details>`/`<summary>` natif, sans une ligne de script | Un script déclaré bloc par bloc : l’invariant 5 l’autorise, mais rien ici ne le paie — le repli natif se referme au chargement suivant, qui est de toute façon un aller-retour serveur. La case à cocher détournée : même résultat, avec une sémantique que les lecteurs d’écran n’annoncent pas |
| D114 | Le pied de page ne ressaisit aucune coordonnée : nom du site, liens éditables, et la ligne « © année » | Adresse, téléphone et raison sociale au pied : une seconde source de vérité pour des faits qui vivent déjà dans les mentions légales (D101), et deux saisies qui divergeraient. Le nom du site reste écrit en toutes lettres même sous un logo : le texte alternatif d’une image n’est pas du contenu indexé |
| D115 | Le rang du titre d’une section est une prop `heading` décidée par la page : la première section visible porte le `h1`, et `check --build` avertit d’une page qui n’en a pas | Le nom du site en `h1` dans l’en-tête : toutes les pages porteraient le même titre, qui n’en décrirait aucune. Un drapeau dans le schéma du bloc : le rang dépend de la place dans la page, pas du type de section. Ne rien vérifier : le titre d’une section est facultatif, et une page sans `h1` reste invisible à l’œil |
| D116 | `f.boolean`, `f.email` et `f.tel` n’entrent pas au DSL | Les ajouter maintenant : sept fichiers à toucher pour zéro consommateur — D99 a déjà tranché ce cas en refusant `f.prose()` — et une forme de stockage devinée qui coûterait une migration le jour où elle est fausse. Ce qui les ferait revenir : un champ du chrome ou d’un bloc qui affiche ou masque une ligne, ou qui saisit une adresse que rien ne valide |
| D117 | Le recadrage est une **ingestion** : une image recadrée est une nouvelle entrée du manifeste, dérivée de l'originale, qui reste | Recadrer en place : on ne peut plus recommencer, une même photo ne peut plus servir à deux formats, et toute page qui citait la clé change d'image sans l'avoir demandé |
| D118 | Le point focal **reste** à côté du recadrage : l'un donne le format, l'autre dit où est le sujet à l'intérieur de ce format | Le remplacer, comme `panel.md` le laissait entendre : même recadrée, une image est re-cadrée par le CSS d'un support à l'autre, et `object-position` reste le seul moyen de dire ce qui ne doit pas sortir du cadre |
| D119 | Le `ratio` d'un champ reste **une valeur**, jamais une valeur par support | `ratio: { mobile, desktop }` : deux images stockées par champ pour une différence de présentation que le CSS et le point focal portent déjà, et deux recadrages à demander au client pour une seule photo |
| D120 | Les faits de l'entreprise ont une source structurée, `content/business.json`, éditée depuis « Édition » | Renverse D101, dont la prémisse tombe : les faits avaient un seul lecteur, ils en ont deux depuis que le JSON-LD existe, et celui-là ne sait pas lire de la prose. Un sixième écran de panel reste refusé (D63) — la fiche est une entrée du sélecteur de page, comme le chrome |
| D121 | Les données structurées d'un bloc sont une fonction pure `structured` de son `schema.ts`, rendues dans le `<head>` par le Layout | Un `<script type="application/ld+json">` écrit dans le composant : la variante bureau devrait le recopier, et deux copies divergent — c'est précisément ce que le contrat des deux rendus interdit (D108). Les fonctions ne survivant pas au JSON du registre, le module généré importe les schémas qui en portent une, comme il importe les composants |
| D122 | Les redirections sont déclarées dans `site.config.ts` et rendues en **pages statiques** par le build | Une règle du `Caddyfile` : il est écrit à l'`init` et n'est jamais régénéré (D106), si bien qu'une redirection ajoutée après coup n'atteindrait jamais la machine — l'irréversible que D98 interdit. Une page à rafraîchissement instantané est lue par Google comme une redirection permanente, et elle repart à chaque mise en ligne |
| D123 | La page 404 est une page du socle, rendue dans les deux rendus et servie par un `handle_errors` du `Caddyfile` | La page nue de Caddy : sans direction artistique, sans navigation et sans un lien pour repartir. Elle est injectée une fois par support, et son adresse dit laquelle est laquelle — Astro écrit `404.html` à plat pour la route racine et `_desktop/404/index.html` pour l'autre. Un dépôt plus ancien est averti par `check --build`, comme il l'est pour l'aiguillage |
| D124 | L'image de partage est un champ de `meta`, et retombe sur la première image de la page | Une image de site unique : la carte d'une page de contact montrerait le bandeau de l'accueil. Le champ déclare `1200/630`, le format qu'attendent les messageries, et l'outil de recadrage est ce qui permet de l'obtenir — un champ facultatif, donc aucune migration (D43) |
| D125 | Le socle porte son propre amorçage de session Claude Code : `.claude/hooks/session-start.sh`, gardé par `CLAUDE_CODE_REMOTE` | Laisser la session cloud se débrouiller : Node y est plus ancien que `.nvmrc`, et `engine-strict=true` transforme l'écart en refus d'installer — rien ne se lance, et la cause n'est pas lisible. Un `npm install` plutôt que `npm ci`, comme le veut l'usage d'un hook de démarrage : il élaguerait du lockfile les binaires de l'autre plateforme, la panne même que `lockfile:check` surveille. Le `.claude/` que `src/client/` génère est celui d'un dépôt client, jamais celui-ci |
| D126 | La notification hors email est un **webhook sortant**, déclaré par `LEAD_WEBHOOK_URL` et gouverné par sa seule présence — aucune capacité ne le commande | Une notification poussée sur le panel : un service worker, une dépendance de chiffrement, des clés à engendrer, une table de plus, et sur iOS une installation sur l'écran d'accueil à expliquer au client — qui ne reçoit rien tant qu'il ne l'a pas faite. Une capacité dans `site.config.ts` : elle rendrait déclaratif ce que la présence d'une variable dit déjà, et un site pourrait déclarer un canal qu'il n'a pas |
| D127 | Le corps du webhook porte le **message entier**, sous `text` et `content` à la fois, à côté des champs structurés | Un `text` seul : Discord lit `content` et n'afficherait rien. Une simple alerte renvoyant au panel : plus propre au regard du RGPD, mais le client rouvrirait le panel pour chaque message, et le canal ne remplacerait pas l'email — ce que la phase lui demande. L'adresse IP et le navigateur du visiteur n'y entrent pas : aucun service au bout n'en a l'usage, et ils ne sortent déjà pas du panel |
| D128 | `Delivery` gagne **`skipped`** : le panel n'avertit que d'une notification réellement manquée | Une colonne par canal : deux états à tenir, une migration, et un vocabulaire de plus au panel pour une nuance que la sortie d'erreur porte déjà. Garder deux états : un site qui ne prévient personne affichait « non transmis par email » en orange sur chaque message — une alarme qui ne veut rien dire |
| D129 | `doctor` sonde SPF, DKIM et DMARC sur le **domaine qui expédie** ; **DKIM refuse**, SPF et DMARC avertissent, et un sélecteur inhabituel se déclare dans `site.config.ts` | Faire refuser SPF, comme le veut l'usage : Brevo expédie sous son propre domaine d'enveloppe et sa documentation dit de ne pas ajouter d'`include` — refuser sur son absence bloquerait un site correctement configuré. Tout avertir : une signature manquante n'est pas un détail de délivrabilité, c'est un code de connexion en spam et un client dehors (D9). Deviner les sélecteurs sans pouvoir les déclarer : ils dépendent du compte, et la sonde refuserait un domaine signé |
| D130 | Le **second facteur reste l'email** (D9 tient), et un webhook n'est pas un canal d'authentification | TOTP ou passkeys : une phase à soi, alors que ce qui manquait à l'email était la preuve qu'il arrive, désormais faite. Faire porter le code au webhook : il aboutit dans une conversation que plusieurs personnes lisent, et un second facteur partagé n'en est plus un. Le secours hors email reste `basalte admin:login` en SSH |
| D131 | Pas d'**expéditeur mutualisé** : les emails d'un site partent de son domaine | Un sous-domaine du mainteneur : il supprimerait toute configuration DNS chez le client, au prix d'un message de client qui ne part pas de chez lui — et il ôterait la raison d'être des sondes qui rendent son domaine utilisable |
| D132 | Un envoi réussi mène à **`/merci` quand le dépôt porte la page** ; absente, la réponse reste le fragment, et un refus ne quitte jamais le formulaire | Complète D76 sans le renverser. Une page de remerciement obligatoire : un site plus ancien y serait envoyé sans l'avoir écrite, et aucune migration ne peut créer un fichier de contenu. Un réglage dans `site.config.ts` : deux choses à tenir accordées, là où la présence du fichier décide seule et se défait en le supprimant. Envoyer aussi un refus : le visiteur perdrait le formulaire où recommencer |
| D133 | Une **page de service** — `/merci` — est écartée du menu déduit, du sitemap et de l'index par un prédicat unique de `src/content/naming.ts` | Trois conditions écrites à la main : trois endroits où une exception s'oublie, exactement ce que D110 reproche, et l'oubli serait muet — une page de remerciement indexée n'a l'air de rien |
| D134 | Les phrases qui manquaient au client se posent dans les **écrans existants**, et l'adresse à qui écrire vient d'`EMAIL_ADMIN` | Un écran d'aide : ce serait la sixième page que D63 refuse, et personne ne l'ouvrirait — les questions se posent là où la limite se rencontre. Une variable ou un champ de configuration pour le support : l'adresse du mainteneur est déjà dans le `.env`, et une seconde source divergerait |
| D135 | Les conventions de code sont vérifiées par une **commande à part**, `basalte lint`, et non par `basalte check` | Les fondre dans `check` : il tourne à l'enregistrement d'un client dans le panel, où un défaut de style qu'il ne voit pas et ne saurait corriger l'empêcherait de rectifier un texte. Les deux n'ont ni le même objet — du contenu contre des schémas, du code contre des règles — ni le même public. D36 tient : le linter généraliste reste écarté, ces règles parlant de blocs, de tokens et de schémas que seul ce dépôt connaît |
| D136 | Ce que le site contient se relève par une **commande à part**, `basalte content`, à côté de `basalte inventory` | Un drapeau sur `inventory` : les deux n'ont ni la même source ni la même fraîcheur — l'inventaire est généré depuis le code et ne bouge qu'avec la version du socle, le relevé vient du contenu et change à chaque enregistrement du client. Une sortie de `check` : il est une barrière, et lui faire porter un rapport de trente lignes le rendrait illisible là où il sert, en pré-commit |
| D137 | `basalte content` **n'écrit aucun fichier** : pas de `--agent` | Un `.claude/contenu.md` régénéré comme `basalte.md` (D89) : il serait faux dès le premier enregistrement du client, et un fichier faux coûte plus qu'un fichier absent — l'agent le croirait |
| D138 | `meta.description` devient **requise**, comme `meta.title` l'était déjà | Un avertissement : il se voit à chaque `check` et finit par ne plus se voir, alors que le champ est sous les yeux du client au moment où il édite la page, avec son aide — c'est exactement le défaut que la frontière « corrigeable depuis le panel » désigne comme refusable |
| D139 | Un titre ou une description repris d'une page à l'autre **avertit**, il ne refuse pas | Refuser : le panel n'enregistre qu'une page à la fois (`savePage`) et ne verrait donc jamais le doublon ; refuser dans `check` seul ferait découvrir le refus à la publication, sur un défaut créé la veille et que rien n'avait signalé. C'est la règle générale : ce qui refuse doit être visible à l'instant où le client le crée |
| D140 | Le **texte alternatif reste sur l'entrée du manifeste**, saisi une fois par image dans la médiathèque | Un `alt` traduisible dans `f.image` : il serait à ressaisir à chaque emploi de la même image, et un type de champ est un engagement de migration (D116). La contrainte ne manquait pas : `references()` refuse déjà une image dont l'`alt` est vide dans une langue en ligne, sur les pages, le chrome et la fiche |
| D141 | Les contrôles de trouvabilité se lisent sur le **contenu**, jamais sur le HTML construit | Les mesurer après le build, comme `checkHeadings` (D115) et le contrat des deux rendus (D108) : titres, descriptions et images de partage sont dans les JSON, les lire après Astro les mettrait dans la moitié de `check` qui ne rend jamais d'erreur, et les rendrait indisponibles à `basalte content`, qui ne construit rien |
| D142 | La publication d'une version du socle est une **commande**, `basalte release`, et non une skill qui enchaîne des gestes | Une skill : elle ne sait que rappeler, or la panne à empêcher — bumper sans taguer — est exactement celle qu'un rappel n'empêche pas, puisqu'elle se produit quand on fait le geste de mémoire. Une commande refuse, et se teste : l'ordre des cinq étapes et le retour en arrière sont éprouvés sur un dépôt jetable. Un script de `scripts/` : il aurait fallu y recopier `readSocle`, `remoteTags`, `readNote` et `tryGit`, c'est-à-dire commettre la duplication que la même phase outille contre |
| D143 | `release` **ébauche la note de version et s'arrête** ; elle ne la remplit jamais depuis les commits | Composer la note à partir des sujets de commit : une note dit l'effet sur un site existant, jamais le travail accompli dans le socle, et une note engendrée dirait précisément le second. Les sujets sont donc affichés au terminal comme matière à traduire, et le gabarit écrit reste invalide — sa ligne « Action requise » refuse tant qu'elle n'est pas portée — plutôt que d'être valide et faux |
| D144 | Le push de publication **nomme les deux références** et les fait avancer par `--atomic` ; le tag est annoté | `git tag vX.Y.Z` suivi de `git push --follow-tags`, ce que `mise-a-jour.md` et le README prescrivaient : `--follow-tags` n'emporte que les tags **annotés**, si bien que la procédure poussait la branche et laissait le tag derrière — elle produisait la panne discrète qu'elle décrivait. Le banc l'a constaté sur un dépôt jetable, personne ne l'aurait vu à la lecture |
| D145 | La revue de réutilisation reste un **jugement porté par une skill**, jamais une règle de `lint` ni un agent | Une règle mécanique : mesurée sur ce dépôt, vingt-et-un noms de fonctions exportées apparaissent deux fois sous `src/`, dont la grande majorité est l'appariement voulu entre une route du panel et son appel côté navigateur — une règle à ce taux de fausses alertes s'ignore, ce qui est pire que pas de règle. Un agent : même lecture, même absence de test, pour un artefact plus lourd qu'une skill |
| D146 | Une skill de ce dépôt-ci ne porte que ce que **ni le typecheck ni un test ne tiennent** | Une skill par geste, à l'image des six qu'un dépôt client reçoit. Ajouter un type de champ `f.*` semblait en mériter une : le compilateur nomme déjà chaque endroit à toucher — `CONTROLS` est un `Record<FieldKind, …>` — et `define.test.ts` compare `FIELD_TYPES` aux clés de `f`. Une skill n'y aurait rien ajouté qu'une récitation, et une récitation vieillit sans que rien ne le dise |
| D147 | Un bloc écrit pour un dépôt client **se recopie** vers un autre ; rien ne le fait circuler installé | Une bibliothèque de blocs installée par tag, comme le socle : un dépôt de plus à publier, à monter de version et à tenir compatible, et surtout un paquet git de plus sur chaque VPS — `environnement.md` le chiffre : clone, `npm install --force` complet dans ce clone, devDependencies comprises, aucun `integrity`, rejoué à chaque build Docker. Installer le bloc depuis le dépôt d'un autre client : deux sites liés, ce que D1 sépare précisément. Le besoin ne disparaît pas, il change d'adresse — c'est le socle qui l'absorbe (D148) |
| D148 | Un bloc de référence gagne sa place s'il démontre une mécanique qu'aucun autre ne montre **ou** s'il est une section que la plupart des sites demandent ; la liste passe de sept blocs à quinze | Garder la liste close sur le seul critère de mécanique (D19) : il n'avait de sens que si un bloc pouvait circuler autrement, et D147 vient de l'exclure. Le socle est alors la seule base commune, et un bloc qui n'y est pas se réécrit à chaque site sans qu'aucun correctif n'y redescende — ce que D5 promettait justement d'éviter. Le coût du renversement est borné : un bloc est deux fichiers, `lint` tient les tokens, et un site n'embarque que le CSS des blocs qu'il emploie |
| D149 | Un bloc reçoit la fiche d'entreprise en prop, et `contact-details` n'a que deux champs | Des champs d'adresse, de téléphone et d'horaires dans le bloc : la même information saisie deux fois, qui diverge — D114 l'avait déjà refusée au pied de page. Sans cette prop, la fiche ne servait qu'au JSON-LD : le client saisissait son adresse pour Google, et elle n'apparaissait sur aucune page de son propre site |
| D150 | Aucun des huit nouveaux blocs ne déclare de `structured` ; `faq` reste le seul | Baliser les témoignages en `Review`, les étapes en `HowTo` et les formules en `Offer` : le premier est un balisage auto-décerné, que Google sanctionne quand il porte sur l'entreprise qui l'héberge ; le deuxième n'a plus de résultat enrichi depuis 2023 ; le troisième exige un prix chiffré et une devise, là où `price` est un texte que le client écrit comme il l'affiche (« à partir de 890 € ») |
| D151 | Le client crée, écrit et supprime des **billets** ; il ne crée toujours pas de pages | Renverse D4 — « pas de blog » — et borne D3, dont la raison tient toujours : elle refusait la liberté parce qu'elle « impose de gérer URLs, navigation et sitemap, et fragilise la DA ». Un billet ne fragilise rien de tout cela — son adresse vient de son titre puis ne bouge plus, il n'entre jamais dans le menu, le sitemap le traite en un seul jeu homogène, et sa mise en page appartient au socle |
| D152 | Un billet **compile en `Page`** avant le rendu ; il n'a ni route, ni enveloppe, ni sitemap à lui | Un pipeline de rendu parallèle : neuf mécaniques à redoubler — `getStaticPaths`, hreflang, carte de partage, contrat des deux rendus, plan de titres, aperçu, JSON-LD, `check --build`, flux — et neuf endroits où elles auraient divergé une par une. La compilation tient en une fonction pure, `pageOfPost` |
| D153 | Le **gabarit d'un billet est un emplacement remplaçable**, parcouru en mode `replace` comme le chrome ; le bloc de **liste**, lui, est un bloc ordinaire | Un bloc de plus dans la bibliothèque : le client pourrait poser « billet » au milieu d'un accueil, où il n'a aucun sens. Un gabarit figé dans le socle : un dépôt client ne pourrait plus redessiner ses billets sans recopier du code (invariant 8) |
| D154 | Un **sixième écran** de panel, « Actualités », affiché seulement sur un site qui déclare un journal | Borne D63 sans la renverser : son argument était « une page vide est une page de trop », et un journal n'est pas vide. `panel.md` fait suivre la hiérarchie à la fréquence d'usage — qui poste tous les jours vient là plus souvent que partout ailleurs. Une entrée du sélecteur de « Édition » : c'est un menu déroulant, inutilisable à trente billets, et sa mise en page trois colonnes est faite pour des sections |
| D155 | **`f.date`**, dixième type du DSL, stocké en « AAAA-MM-JJ », jamais traduisible, rendu par le champ natif du navigateur | Une date dans l'enveloppe du billet, hors du système de champs : le formulaire du billet aurait porté le premier contrôle écrit à la main d'un panel qui n'en a aucun. `@mantine/dates` : un paquet de plus sur chaque VPS (D57) pour ce que le contrôle natif fait déjà, en rendant justement « AAAA-MM-JJ » |
| D156 | Un billet se masque **par langue**, par le même `hidden` qu'une section ; « brouillon » est un billet masqué dans toutes les langues en ligne | Un champ `status` distinct : deux notions à tenir là où le socle en a déjà une, et un billet rédigé dans une seule langue aurait fait échouer `check` sur l'autre langue en ligne. Le masquage sort aussi le billet de `getStaticPaths` — un brouillon n'a d'adresse nulle part, là où une page masquée garde la sienne |
| D157 | Un billet en **brouillon se valide sans ses bornes basses** ; les retrouver est ce que coûte le fait de paraître | Exiger un billet complet dès sa création : le client ne pourrait pas l'écrire en plusieurs fois, ce qui est précisément le geste quotidien qu'on lui rend possible. Rendre les champs facultatifs pour de bon : un billet publié pourrait sortir sans texte ni résumé, et D138 tomberait. C'est la règle des langues en préparation (D18), portée sur l'axe du brouillon |
| D158 | Le titre et le résumé d'un billet **deviennent** le titre et la description de sa page ; l'image de partage, elle, reste vide | Recopier la couverture dans `meta.image` : elle aurait été mesurée contre le `1200/630` que `meta` déclare, alors qu'une couverture d'article est en `16/9` — chaque billet aurait porté un avertissement que rien ne permettait de corriger. La carte retombe sur la couverture par le chemin ordinaire (D124) |
| D159 | Ni pagination, ni rubriques, ni publication programmée | La pagination : des routes, des canoniques et des `prev`/`next` pour un journal qui n'existe pas encore ; l'index porte tout, groupé par année. Les rubriques : des pages maigres que Google sanctionne. La programmation : un site statique n'a pas d'horloge — un billet daté de demain n'apparaîtrait qu'à la construction suivante, donc jamais |
| D160 | Une **borne haute de liste n'existe que si la mise en page casse sans elle** — une seule dans le socle, le menu de l'en-tête. Partout ailleurs elle sort, et `basalte lint` réclame la phrase qui justifie celles qui restent | La garder comme borne éditoriale assumée : dix bornes sur onze ne tenaient à rien de vérifiable, et personne n'aurait plus su quand en poser une — chaque bloc écrit aurait payé l'hésitation. La sortir du DSL : le menu de l'en-tête est une rangée sans repli, et plus rien ne l'aurait tenu. C'est la règle du `<script>` dans un bloc (D135), portée sur les bornes : aucune machine ne juge si elle est méritée, elle demande qu'on le dise |
| D161 | La **borne basse ne suit pas la haute**. `min` dit autre chose : le caractère requis s'exprime par elle (D43), et `min: 2` sur les chiffres clés ou les étapes est sémantique — un seul chiffre n'est pas une section de chiffres | Les traiter ensemble par symétrie : on aurait perdu une contrainte de sens pour corriger une contrainte de mise en page, deux choses sans rapport. La symétrie n'est pas une raison |
| D162 | Le **poids d'une page se mesure sur le HTML construit et avertit**, jamais ne refuse. Ce qui est compté est ce qu'un navigateur télécharge à l'ouverture : le HTML, les feuilles de style, et la plus large dérivée de chaque image | Un refus au-delà du budget : le client le découvrirait à la publication, sur du contenu écrit la veille (`seo-performances.md`), et il n'existe pas de poids au-delà duquel une page cesse de fonctionner. Ne rien mesurer : c'était la seule chose que les bornes tenaient vraiment, et personne n'aurait vu une galerie de soixante photos avant la mise en ligne |
| D163 | Dans le panel, les éléments d'une liste sont **repliés, un seul ouvert à la fois**, et l'ouverture suit l'élément quand la liste se réordonne | Plusieurs ouverts : le ruban illisible revient dès le cinquième déplié. Ne replier qu'au-delà d'un seuil : le même geste se comporterait de deux façons selon la longueur de la liste. Laisser l'ouverture sur son rang : après un déplacement, l'élément ouvert n'aurait plus été celui qu'on lisait — les identifiants d'une liste sont des rangs, pas des identités (D10 ne vaut que pour les sections) |
| D164 | `basalte lint` contrôle aussi la **feuille et les tokens du panel** : `hardcodedStyle` apprend dans quel système il lit, et `panelContrast` réemploie `contrast()` sur les paires que le panel superpose vraiment | Corriger les valeurs sans poser la règle : vingt-et-une valeurs hors échelle et deux paires sous le plancher s’étaient installées précisément parce que rien ne regardait `src/admin/`, pendant que `panel.md` écrivait « Le plancher est vérifié ». Un test unitaire seul : il aurait tenu le contraste et rien dit des `15px` et des `gap: 11px`, qui sont la même faute sous une autre forme. Les tokens quittent `theme.ts` pour un module pur — la commande est du Node, `@mantine/core` du navigateur |
| D165 | Le panel **vouvoie**, et `docs/conventions.md` le dit | Laisser les deux cohabiter : six chaînes tutoyaient déjà, dont « Réessaie dans un instant », la plus vue de toutes. Une voix qui change d’un écran à l’autre se lit comme deux personnes qui parlent, et rien ne disait laquelle avait raison |
| D166 | Une **erreur de validation atteint le champ qui la cause**. `ContentIssue` gagne le chemin machine, le refus le transmet entier, chaque niveau du formulaire retire le segment qu’il porte, et chaque ligne du résumé ouvre la section qu’elle nomme | La liste de phrases : le serveur connaît déjà la section, le champ et la langue, et les aplatir obligeait le client à relire son écran pour retrouver lequel — sur une liste répétable de trente éléments, à tout déplier. Valider dans le panel : ce serait une seconde source de vérité à côté des schémas, ce que D58 refuse. Le chemin ne remplace pas `field`, qui reste le libellé d’une phrase : deux champs peuvent porter le même, et un rang d’élément n’a pas de nom |
| D167 | La barre du haut **se replie à 80 rem**, ses onglets sur leur ligne et défilants ; les trois colonnes s’ordonnent liste, réglages, aperçu sous 75 rem | Replier au point tactile (60 rem) : la barre demande environ mille deux cent cinquante pixels et un portable courant en a mille deux cent quatre-vingts — entre les deux, la marque se coupait en deux et les premiers onglets passaient sous les derniers. Un menu replié : ce serait un troisième niveau de navigation, que `panel.md` interdit. Laisser l’aperçu au milieu de la pile : il mettait vingt-quatre rem entre la liste des sections et leurs réglages |
| D168 | L’**aperçu d’un billet le montre démasqué** | Reporter le masque : `pageOfPost` le pose sur l’unique section de la page compilée, si bien qu’un brouillon n’avait aucune section visible et que l’aperçu montrait un en-tête et un pied séparés par du vide — au moment précis où on l’écrit. Le masque décide de ce qui part en ligne, pas de ce que son auteur peut relire |
| D169 | Ce que le panel explique vit sous un **« ? » par écran**, déplié à la demande, et dans un seul module (`src/admin/Help.tsx`) | Les phrases posées en permanence dans les écrans (D134) : elles occupaient huit paragraphes gris sur les six écrans, tous les jours, pour expliquer des boutons à quelqu’un qui les connaît depuis le deuxième. D134 tenait sur le fond — la réponse reste là où la question se pose, et le « ? » est sur l’écran même — mais pas sur la forme : une réponse qu’on n’a pas demandée est du bruit dès la deuxième visite. Un écran d’aide : ce serait la septième page que D63 refuse, et il faudrait l’ouvrir en sachant qu’elle existe |
| D170 | Le panel porte **un accent vif**, et deux éléments accentués par écran au plus : l’accent plein pour l’action qui change l’état du site, son lavis pour l’action propre à l’écran | Le noir plein comme action principale, et le bleu réservé aux liens : l’outil était monochrome, et rien n’attirait l’œil vers ce qu’on vient y faire. Accentuer davantage : à trois éléments colorés, plus aucun ne ressort — c’est la hiérarchie qui se perd, pas la couleur qui manque |
| D171 | Les **rayons sont divisés par deux** (6/8/10/14), et la forme pleine ne reste qu’au badge, à la pastille et à l’icône ronde ; les quatre surfaces sont **creusées** pour que la carte blanche ressorte | Les rayons d’avant : un panel entier en pastilles se lit comme un jouet, et un angle de vingt-quatre pixels mange le coin d’une carte qui n’a que seize pixels de marge. Les surfaces d’avant : quatre gris à moins de dix pour cent l’un de l’autre, sur lesquels une carte blanche ne se détachait pas — c’est ce qui faisait l’écran fade |
| D172 | Le panel **dessine un filet d’un pixel** (`#ebebed`) pour séparer deux plans, et l’ombre ne reste qu’à ce qui flotte réellement au-dessus du reste — menu, barre posée sur le canvas, fenêtre | Renverse D97, « aucune bordure », dont l’argument était que « sur un écran qui empile listes, cartes et panneaux, les traits s’accumulent et durcissent l’outil ». Ils s’accumulent en effet — mais l’absence de trait obligeait à séparer par la valeur, donc à creuser quatre gris à moins de dix pour cent les uns des autres, sur lesquels une carte blanche ne se détachait pas : c’est D171 qui avait déjà dû corriger cela, sans y parvenir. Du gris sur du gris avec des ombres seules ne fait pas une hiérarchie, il fait une bouillie. Le trait rend l’arête nette et rend au fond son unique gris |
| D173 | La **forme pleine devient le défaut** : tout ce qui agit, étiquette ou compte s’arrondit complètement. Trois familles gardent l’arête, et elles seules — le champ et la ligne de liste, parce qu’une colonne se lit sur un axe vertical net, et la surface, parce qu’on ne manipule pas une carte. Quatre rayons : 8, 10, 12, 14 | Renverse D171, « les rayons sont divisés par deux, la forme pleine ne reste qu’au badge, à la pastille et à l’icône ronde », qui craignait qu’« un panel entier en pastilles se lise comme un jouet ». Il se lit comme un jouet quand la pastille est partout, y compris là où l’on aligne une colonne ; il se lit comme un outil quand l’exception est nommée et tient à une raison de lecture. Garder l’échelle 6/8/10 : deux pixels d’écart ne se voient pas, et une échelle qu’on ne distingue pas n’est pas une échelle |
| D174 | **L’action est noire.** Le bouton qui change l’état du site, l’onglet ouvert et la marque sont en `#17171a` ; l’accent pétrole ne dit jamais « fais », il dit « voici ce que tu modifies » et « voici ce qui se mesure » | Renverse D170, « le panel porte un accent vif, deux éléments accentués par écran au plus », dont l’argument était que « l’outil était monochrome, et rien n’attirait l’œil vers ce qu’on vient y faire ». Le défaut était réel, le remède faux : un accent qui porte l’action entre en concurrence avec les couleurs du site que le client édite dans l’aperçu, à trente centimètres. Le noir n’a pas ce problème, et l’accent libéré sert enfin à quelque chose que rien d’autre ne disait — la sélection et la donnée. La limite de deux éléments accentués tombe avec : ce n’est plus le nombre qui tient la hiérarchie, c’est le rôle |
| D175 | **Mantine sort du panel.** Les composants sont écrits dans `src/admin/ui/`, sur `panel.css` et la couche de tokens | Amende D7, D57 et D65, qui posaient Mantine comme la bibliothèque du panel et refusaient d’y ajouter des dépendances. Rethémer : trente composants sur vingt-quatre fichiers, chacun défait un à un dans `theme.ts` pour obtenir une pastille sélective, une hauteur de trente-deux et un filet d’un pixel — c’est écrire la direction artistique deux fois, une fois en CSS et une fois contre les défauts d’une bibliothèque, et la seconde se défait à chaque montée de version. La moitié de D65 qui compte survit et se renforce : le panel n’emprunte toujours rien aux tokens du site |
| D176 | L’**aperçu passe à gauche** et occupe la place ; la liste des sections et les réglages de celle qui est choisie s’empilent dans une colonne de 388 px à droite. Ce qui commande l’aperçu flotte au-dessus de lui : le support regardé, la page ouverte, son adresse | Renverse la mise en page de D96, dont le fond reste vrai — l’aperçu est le centre de l’écran, et il montre le dernier enregistrement. Trois colonnes : elles mettaient vingt-quatre rem entre la liste d’une section et ses réglages, si bien qu’en choisir une obligeait à traverser l’aperçu du regard pour la modifier. Le sélecteur de page dans l’en-tête : l’en-tête dit l’écran, pas la page — sa place est contre le lien qui ouvre cette page |
| D177 | Le **plancher du dessin ne vaut que pour ce qui porte une information** : la barre d’un histogramme, une jauge, l’anneau de focus. Le filet qui sépare, la poignée au repos et le glyphe inerte en sont tenus dehors, nommément, et `panel.test.ts` vérifie qu’ils y restent | Précise D164, qui mesurait « le trait dessiné » sans distinguer. Tout mesurer : il aurait fallu porter le filet et la poignée à 3:1, soit un gris moyen — le trait d’un pixel serait redevenu la bordure épaisse que D172 vient d’éviter, et la poignée aurait pesé autant que le titre qu’elle borde. Ne rien mesurer : c’est l’état d’avant D164, où une barre d’histogramme à 1,6:1 rendait le graphique illisible sans que rien ne le dise. La ligne est le rôle, pas la nature : la poignée se donne au survol et au focus, et une section se déplace aussi au clavier |
| D178 | Le **recadrage disparaît** : le point focal, réglé une fois dans la médiathèque, est le seul réglage d’image, et le site cadre autour de lui quelle que soit la forme de l’emplacement | Garder les deux : le client avait à choisir entre deux gestes qui font la même chose, et la fenêtre de recadrage était le seul endroit du panel où l’on dessinait à la souris. Un recadrage fige une forme, alors que la même image sert un bandeau en 16/9 et une vignette en 4/3 : le point focal les sert toutes les deux, et il se corrige sans réencoder |
| D179 | Le client **ajoute une section** à une page, depuis la bibliothèque des types que le site déclare | Renverse la moitié de D3 qui refusait l’ajout, dont l’argument — « impose de gérer URLs, navigation et sitemap, et fragilise la DA » — ne vaut que pour la **page** : une section n’a ni adresse, ni place dans le menu, ni entrée de sitemap. Elle vit dans le JSON d’une page qui existe déjà, et son schéma la borne. Créer une page reste refusé, et pour la raison d’origine, intacte |
| D180 | Un site neuf démarre avec **douze images**, ingérées comme un téléversement | Un `public/media/` vide et un `content/media.json` à `{}` : la première chose que le client voyait de son site était un emplacement gris, et il ne pouvait rien juger de sa mise en page avant d’avoir photographié son atelier. Les recopier telles quelles : elles échapperaient à l’invariant 3, et un dépôt neuf porterait des fichiers qu’un téléversement n’aurait pas pu écrire |
| D181 | Le panel emploie **Geist**, en fonte variable **auto-hébergée** | La pile système : elle change de dessin d’un poste à l’autre, et la graisse 450 qui porte les lignes de liste n’y existe nulle part. Google Fonts : le panel est servi depuis le VPS du client, en France, et appeler un tiers à chaque ouverture d’une interface d’administration ajoute une dépendance réseau et une trace, pour deux fichiers de soixante-dix kilo-octets qu’on peut livrer |
| D182 | Les couleurs portent **trois plans** : `bg`, `surface` (le retrait) et `contrast` (le plan sombre, avec son `contrastFg`) | Garder le seul couple fond/accent : `cta` détournait `--color-accent` pour son bandeau inversé, si bien qu'un site qui voulait ce bandeau noir devait écrire `accent: '#16181d'` — et n'avait plus d'accent nulle part. Le site de démonstration en était là. Un plan sombre rendu à lui-même rend l'accent à sa fonction, et `surface` remplace le trait qui séparait deux sections |
| D183 | L'échelle s'étend de onze valeurs — `text-4xl`, `text-5xl`, `space-9`, `space-10`, `radius-lg`, `radius-xl` et les trois couleurs de D182 — et d'aucune de plus. **Pas de famille `shadow`** | Laisser l'échelle en l'état : `--text-3xl` (2,5 rem) était le plus gros caractère du socle, au téléphone comme sur un 27 pouces, et `--space-8` (4,5 rem) son plus grand intervalle : aucune page ne pouvait respirer. Ajouter une ombre : D172 a tranché pour le panel — « l'ombre ne reste qu'à ce qui flotte » —, et le seul élément qui flotte ici est l'en-tête collant, qu'un filet d'un pixel sépare mieux |
| D184 | Une valeur qui doit varier avec l'écran se compose de **deux tokens et d'un `vw`**, dans un `clamp` — jamais d'une media query par bloc | Une taille par palier : chaque bloc aurait porté ses propres points de rupture, et la direction artistique aurait cessé d'être pilotable depuis `site.config.ts`. Le `clamp` garde les deux bornes en tokens, et `basalte lint` l'accepte parce qu'aucune longueur n'y est écrite : `vw` n'est pas une unité qu'un token porte |
| D185 | Le **plan d'une section vient de son type**, jamais d'un champ : `logos`, `stats`, `bento`, `testimonials`, `faq` et `contact-details` sont en retrait, `cta` est inversé, le reste est sur le fond | Un sélecteur « fond » sur chaque bloc : dix-neuf occasions de casser la direction artistique, ce que D3 refuse depuis le premier jour — D179 n'a ouvert que l'**ordre** des sections, pas leur habillage. Alterner automatiquement selon le rang : deux sections qui n'ont rien à voir se seraient trouvées liées par leur seule position |
| D186 | Le **bouton vit dans `src/astro/base.css`**, en trois formes (`.button`, `--quiet`, `--invert`), et `basalte lint` contrôle désormais cette feuille et le gabarit de billet | Le laisser dans chaque bloc : six blocs en portaient un, chacun avec son dessin et son survol — le même geste se lisait comme six. Le poser hors du contrôle : la feuille commune serait devenue la seule porte par laquelle une couleur en dur entre dans une page. `src/journal/post/` échappait au lint pour la même raison, et y entre du même geste |
| D187 | L'**en-tête est collant**, et son voile se compose de deux tokens par `color-mix`, sous un `@supports` qui l'opacifie là où le flou n'existe pas | Une barre qui défile avec la page : sur une landing, le seul chemin vers l'action disparaît au premier écran passé. Un voile en couleur écrite : la direction artistique cesserait d'être pilotable, et `lint` le refuserait. Un voile sans garde : translucide sans flou, le texte de la page traverse la barre et plus rien ne se lit |
| D188 | Trois blocs de référence de plus — `showcase`, `bento`, `comparison` —, et le comparatif porte **deux colonnes, pas un nombre libre** | Le critère de D148 les fait entrer : `showcase` est la section « une chose expliquée » qu'aucun bloc ne portait, `bento` démontre une liste dont chaque élément décide de sa place, `comparison` est une section que la plupart des pages produit demandent. Un comparatif à N colonnes : une liste de valeurs par ligne devrait rester alignée sur une liste de colonnes, et rien dans le panel ne saurait le tenir |
| D189 | Le site de démonstration **devient la landing de Basalte**, et sa médiathèque ne porte plus que des captures réelles du panel et du site | Garder l'atelier de menuiserie : rien dans le dépôt ne montrait ce que le socle produit vraiment, et la seule page complète qui existait était inventée. Les douze images de départ d'un site neuf ne bougent pas (D180) : elles vivent dans `src/client/media/`, et un artisan reste le bon sujet pour elles |
| D190 | Sous `astro dev`, dans le dépôt du socle, l'**intégration compilée s'efface devant ses sources** : `dist/astro/index.js` importe `src/astro/index.ts` nativement, avec un hook de résolution `module.registerHooks` qui prend `x.ts` quand `x.js` manque sous `src/`, et lui délègue le hook entier. Tout ce que l'intégration désigne — blocs, chrome, routes, island — vient alors de `src/`, que Vite recharge à chaud | Faire importer `src/astro/index.ts` par `astro.config.mjs` : la configuration est lue par un chargeur que Vite referme avant les hooks, et tout `import()` qui suit échoue. Un `tsc --watch` qui tiendrait `dist/` à jour : `rm -rf` puis deux cents fichiers réécrits, c'est ce qui faisait tomber le serveur, et il resterait le redémarrage complet à chaque contenu. Un alias Vite seul : les schémas et les racines de blocs se lisent hors de Vite, à la configuration |
| D191 | Un fichier de `content/` qui change **réécrit le module généré et l'invalide dans le graphe de Vite**, par un plugin qui guette le dossier ; le serveur ne redémarre plus. La page se recharge quand le changement vient d'ailleurs que du panel — qui signe ce qu'il écrit sur `globalThis`, le temps que le guetteur le lise — et pas quand c'est lui qui enregistre. Seul `site.config.ts` reste déclaré à `addWatchFile` | `addWatchFile` sur chaque JSON, comme avant : Astro relance alors `config:setup` entier, relit le projet, réinjecte les routes, et ce redémarrage coupait la requête du panel qui venait d'enregistrer. Laisser Vite guetter le module généré lui-même : il rechargerait toutes les pages, panel compris, à chaque enregistrement |
| D192 | `update-all` **monte chaque site par son propre `basalte update`**, lancé par `npm exec` dans le dépôt du site | Appeler `update()` du socle qui lance la commande : il planifiait depuis sa propre version, si bien qu'un parc en retard sur un mainteneur à jour ne bougeait pas — « déjà en v1.5.0 » sur chaque site —, et qu'une note « manuelle » d'une version intermédiaire passait inaperçue |
| D193 | Enregistrer un écran du panel **ne relit du serveur que cet écran-là** ; le brouillon non enregistré de l'autre reste ce qu'il est, et quitter le panel demande confirmation dès que l'un des deux a bougé | Relire les deux à chaque enregistrement, comme avant : enregistrer une page effaçait le billet en cours d'écriture, sans un mot, et la confirmation de sortie ne regardait que l'écran ouvert |
| D194 | Le panel parle **Material Design 3** : ses couleurs sont des rôles, sa forme une échelle, son texte quinze styles, son élévation des conteneurs tonaux et cinq ombres, ses états une couche d’opacité — écrits à la main dans `panel.css` et `src/admin/ui/`, sans bibliothèque (D175 tient) | Prolonger la direction artistique de la phase 18 : elle était cohérente, mais ne partageait aucun vocabulaire avec ce qu’un client pratique chaque jour sur son téléphone et dans ses outils, et chaque composant demandait d’être appris. `@material/web` : une dépendance lourde pour des composants qu’il aurait fallu rethématiser, et le retour du problème que D175 a réglé |
| D195 | Les couleurs du panel se tirent d’une **graine** : neutre par défaut (`#5c5c60`), et un site en déclare une autre par `panel: { seed }` dans `site.config.ts` | Reprendre `tokens.color.accent` du site : D65 tient — la direction artistique d’un client ne décide pas de la lisibilité de son outil, et un accent choisi pour une landing n’a pas à porter un formulaire. Une graine choisie pour l’outil, dont les tons garantissent le plancher, donne au panel l’air du client sans lui donner sa palette |
| D196 | Le générateur lit la teinte et le chroma en **OKLCH**, et pose le ton en **clarté L\* de CIELAB** — la table de tons de Material, rôle par rôle, dans un module pur (`src/admin/scheme.ts`) | Porter HCT et CAM16 : six cents lignes et des tables pour une garantie qui ne tient qu’à L\* — deux tons séparés de quarante se lisent à 3:1, de cinquante à 4,5:1, quelle que soit la graine, et c’est ce que `lint` mesure. Un OKLCH pur : sa clarté n’est pas L\*, et la garantie tombait |
| D197 | Le panel porte un **schéma sombre** qui suit `prefers-color-scheme`, sans interrupteur | Une préférence dans « Compte » : un réglage de plus à stocker pour ce que le système d’exploitation exprime déjà, et un panel qui contredit le reste de l’écran |
| D198 | Les deux schémas du neutre sont des **littéraux dans `tokens.ts`**, et `scheme.test.ts` les compare à la sortie du générateur | Générer `panel.css` : une étape de build, et la feuille cesse d’être écrite à la main — ce que le test de D95 tient depuis la phase 18. Un littéral vérifié est aussi sûr qu’une valeur générée, et se lit sans rien lancer |
| D199 | La graine d’un site arrive en **`<style>` inline émis par `admin.astro`**, sur `:root[data-seed]` | `setProperty` depuis React : un flash de neutre avant que l’island monte. Un `:root` sans attribut : l’ordre des feuilles dans `<head>` diffère entre `astro dev` et le build, et la surcharge pouvait perdre |
| D200 | Une surface **s’élève par sa couleur** — la surface, puis cinq conteneurs de plus en plus clairs — et l’ombre ne vient qu’à ce qui se détache vraiment : carte élevée, menu, fenêtre, bouton flottant. Remplace D172 pour le panel | Garder le filet d’un pixel : Material n’en a pas, et deux systèmes de séparation dans une même feuille se lisaient comme deux outils. Le site garde le sien, pour sa raison à lui (D183) |
| D201 | Le **bouton plein en `primary` dit « fais »** ; le tonal agit sur l’écran, le contour propose, le texte annule ; le ton d’erreur se pose sur n’importe lequel. Remplace D174 | Garder l’action noire : sur un schéma teinté, un noir pur se lit comme étranger, et Material réserve le plein à l’action première — c’est ce que le client reconnaît |
| D202 | L’**échelle de forme** de Material — 0, 4, 8, 12, 16, 28 et le plein — sous la famille `radius` | La renommer `shape` : la famille du lint est partagée avec `--radius-*` du site, et un nom par système aurait demandé une table de correspondance par propriété |
| D203 | Le **libellé d’un champ se tient au-dessus** du contrôle, qui porte le contour de Material — un filet, qui double et prend la couleur principale au focus | Le libellé flottant de Material : impossible sur un champ composite — une image, un document, une liste, un groupe n’ont pas de bord où le faire flotter —, et il aurait cassé le contrat de D166, où c’est le même libellé qui vise le contrôle pour que l’erreur l’atteigne |
| D204 | La navigation est un **rail** à partir de 840 pixels et une **barre** en dessous, cinq destinations au plus ; **« Compte »** n’en est pas une et se tient derrière l’avatar, avec la déconnexion. Remplace D167 | La barre noire à six onglets : elle ne tenait pas sur un téléphone, et la sixième destination était celle qu’on ouvre une fois par mois. Cinq est le plafond d’une barre de Material, et c’est ce qui a fait sortir le compte de la liste |
| D205 | Ce qui vient de réussir se dit dans une **snackbar** — « Enregistré », « Mise en ligne lancée » — qui s’efface d’elle-même ; ce qui a échoué au niveau du site reste un bandeau | Tout laisser dans la ligne d’œilleton : « enregistré à 14:02 » y change sans qu’on le remarque, et le client cliquait deux fois. Une snackbar pour l’échec aussi : un échec doit rester jusqu’à ce que la cause disparaisse |
| D206 | Le panel emploie **Roboto Flex** et **Roboto Mono**, les sous-ensembles latins variables que Google sert, **auto-hébergés** dans `src/admin/fonts/` | Garder Geist : ce n’est pas la police pour laquelle Material est dessiné, et le panel aurait eu l’air d’une imitation. Appeler Google Fonts : la raison de D181 tient — le panel est servi depuis le VPS du client, et n’appelle pas un tiers pour s’afficher |
| D207 | Les icônes sont des **Material Symbols** recopiés en chemins SVG inline dans `src/admin/ui/icons.tsx`, une trentaine, sous Apache 2.0 | La police d’icônes : deux cents kilo-octets et un texte qui paraît avant ses glyphes. Un jeu dessiné à la main, comme en phase 18 : il fallait le redessiner à chaque écran, et il ne ressemblait à rien que le client connaisse |
| D208 | L’**apparence se règle par appareil**, dans « Compte » : un mode — système, clair, sombre — et une graine à soi, rangés dans le `localStorage` du navigateur avec les deux schémas déjà calculés, et posés sur `<html>` par un script inline avant toute feuille (`src/admin/appearance.ts`). Remplace « sans interrupteur » de D197 | Un réglage côté serveur : une requête avant de savoir quelle couleur peindre, donc un clignotement à chaque ouverture, et une colonne de plus pour ce qui n’est qu’un confort d’appareil — la même personne veut son panel sombre sur le portable le soir et clair sur l’écran du bureau. Recalculer le schéma à l’amorçage : embarquer le générateur inline dans chaque page du panel, pour ce qu’une valeur rangée dit déjà |
| D209 | L’**échelle du panel se resserre d’un cran** sur celle de Material : corps à 13 px, champ à 40 px, bouton à 36 px, icône à 20 px, rangée à 40 px, rail à 72 px ; les contours passent sur `outlineVariant`, un tonal éteint perd son fond, l’état « enregistré » perd son cadre | Garder les tailles de Material : elles sont dessinées pour le pouce, et sur un écran de bureau tout paraissait écrasé — de grands objets serrés les uns contre les autres. Un cadre gris sur un bouton secondaire et un fond gris sur un bouton éteint : deux taches à côté de l’action, là où un filet et une encre estompée suffisent |
| D210 | La **connexion tient en deux volets** : le site à gauche, sur un dégradé tiré de sa graine, son nom en grand ; le formulaire à droite, sur la surface. Sur un écran étroit, le volet devient un bandeau au-dessus du formulaire | Une carte au milieu du vide : c’était le premier écran qu’un client voyait de son outil, et il ne disait rien — ni à qui il appartient, ni ce qu’on y fait |
| D211 | Une **ligne d’interrupteur** — le libellé à gauche, l’interrupteur au bout, sur le conteneur bas — remplace l’interrupteur posé à côté d’un titre ; une ligne de liste de deux hauteurs garde le **rayon moyen**, la forme pleine restant aux menus | L’interrupteur flottait à côté du titre et passait à la ligne dès que la colonne se resserrait. La forme pleine sur un titre de deux lignes et sa date faisait un galet, et le texte en débordait |
| D212 | L’écran d’édition tient en **trois colonnes : la structure à gauche, l’aperçu au centre, le formulaire à droite**. La structure porte la page ouverte et la langue écrite, puis les sections ; le formulaire commence en haut de sa colonne ; « Actualités » a la même forme, ses billets pour structure. Remplace D176 | Deux colonnes, l’aperçu à gauche et une colonne de 360 px qui empilait avertissements, sections et formulaire (D176) : ce qu’on écrit commençait sous la ligne de flottaison. Le sélecteur de page dans la barre de l’aperçu : une puce d’adresse ne se lisait pas comme le menu des pages, et disparaissait sous le formulaire dès que l’écran s’empilait. L’aperçu à droite, entre les deux colonnes de réglage à gauche : la page qu’on modifie est ce que l’écran montre, elle est au milieu et prend la largeur que les deux autres lui laissent — et D176 avait tort de craindre la traversée du regard, puisque l’aperçu défile jusqu’à la section choisie (D217) |
| D213 | L’**aperçu bureau est rendu à 1 024 px et réduit à l’échelle** de sa colonne, par une règle de la feuille — `tan(atan2())` sur la largeur du conteneur — ; le rendu mobile tient à sa taille | Un cadre à la largeur de la colonne : « Bureau » y montrait la mise en page tablette du site, un titre coupé en deux, et disait bureau. Mesurer en JavaScript : un redimensionnement redessinait le panel pour un rapport que la feuille sait calculer. 1 280 px : la réduction rendait le texte illisible, pour une mise en page que 1 024 donne déjà |
| D214 | La barre d’application dit chaque état **une fois** : le titre est le **titre de la page** sans le nom du site — et l’accueil vient en tête du menu —, l’état de mise en ligne est une **marque à côté du titre**, le bouton **« Enregistrer » lit « Enregistré »** quand il n’y a rien à faire ; le bandeau d’échec ne reste que sur les écrans qui mettent en ligne, et le « ? » se referme avec l’écran | La ligne d’œilleton « Édition · dernière tentative le … · sans historique » : trois natures d’information dans une phrase, dont un jargon de développeur. L’état « enregistré » dit trois fois — badge, bouton grisé, heure. Le titre dérivé du nom de fichier, « Actualites », quand le serveur rendait le titre. Un échec de mise en ligne vieux de deux jours en bandeau rouge sur « Compte » et « Messages », où l’on n’y peut rien |
| D215 | Le **pied d’un champ** porte l’aide à gauche et la mesure à droite, sur une ligne ; sous 1 200 px, l’ordre d’empilement est celui de chaque écran — la grille de la médiathèque avant le panneau de l’image, la structure avant le formulaire — ; le compte range son journal sous ses trois cartes, borné à huit lignes, le navigateur en clair | Le compteur entre le champ et l’aide se lisait comme le début de la phrase d’aide. Un `order: -1` global sur le panneau latéral posait « Aucune image choisie » au-dessus de la grille qu’on n’avait pas encore parcourue. Le journal de connexion — deux cents lignes de « Mozilla/5.0 (Window… » et « adresse inconnue » — prenait deux tiers de l’écran à côté du mot de passe |
| D216 | L’**échelle de type remonte d’un cran** sur D209 pour le corps et les étiquettes — corps 14 px, champ 15 px, étiquette 13 px, titres 16, 18 et 22 px —, le **libellé d’un champ porte l’encre pleine**, et l’écran se tient sur le **conteneur bas**, un ton sous la barre et le rail | D209 resserrait tout d’un cran d’après une capture, pas d’après l’usage : le corps à 13 px, les étiquettes à 12 px et les cartes blanches sur une surface presque blanche donnaient un panel qu’on ne lisait pas. Garder les tailles de Material sur les grands styles aussi : les en-têtes à 24 et 28 px poussaient le contenu d’un écran étroit |
| D217 | Choisir une section dans la liste **fait défiler l’aperçu jusqu’à elle et la souligne** ; l’aperçu enveloppe chaque section de son identifiant, et le site construit ne porte pas cette enveloppe. Chaque ligne de la liste montre le **premier texte court** de la section, son type en note | Injecter un script dans l’aperçu pour lui parler : le fragment de l’adresse suffit, et changer le seul fragment ne recharge pas le cadre. Poser l’identifiant dans le site construit : une enveloppe de plus sur chaque page publique, pour un besoin du panel. Quatorze lignes « Grille de cartes » ne disaient pas laquelle on cherchait |
| D218 | L’écran d’édition tient en **deux colonnes** : l’aperçu, et un volet de 400 px. Le choix de la page passe dans la **barre de l’aperçu**, celui de la langue en tête du volet ; le volet montre **une chose à la fois** — la liste des sections, le titre et la description de la page, ou le formulaire de la section choisie —, et un interrupteur segmenté sépare les deux premières. Remplace D212 | Garder la colonne de structure : elle n’était une colonne que parce que la sélection s’y faisait, et la sélection se fait maintenant dans l’aperçu. L’objection de D212 au sélecteur dans la barre — « une puce d’adresse ne se lisait pas comme le menu des pages » — visait une puce muette, qui ne portait que la route ; celle-ci porte le titre de la page et le chevron d’un menu |
| D219 | Le panel et son aperçu se parlent par **`postMessage`, dans les deux sens** : l’aperçu remonte la section cliquée et le rang où l’on demande une section de plus, le panel désigne la section courante et dit si le cadre doit l’amener en vue. Un « + » paraît entre deux sections au survol. Remplace le canal de D217 | Le fragment d’adresse : il ne peut pas porter un clic, il ne rejoue pas quand on rechoisit la même section, et changer le `src` d’un cadre empile une entrée d’historique — que le panel emploie lui-même pour router. Le reste de D217 tient : l’enveloppe `[data-section]` n’existe que dans l’aperçu, et la ligne d’une section montre son premier texte court |
| D220 | « Actualités » est la **liste de ses billets en pleine largeur** — une carte par billet, sa couverture, sa date, sa parution et ce qu’il reste à traduire —, et écrire ouvre un **second niveau** qui remplace le contenu de l’écran : l’aperçu et le volet du billet. Remplace la moitié « Actualités a la même forme » de D212 | Une fenêtre par-dessus tout : elle couvrirait la barre d’application, et il faudrait redonner « Enregistrer » et « Mettre en ligne » dans son pied — deux endroits pour la même action, ce que D214 refuse. Garder les trois colonnes : un billet n’a ni sections à choisir ni ordre à régler, et l’écran passait un tiers de sa largeur à montrer une liste en permanence |
| D221 | Une carte à hauteur définie **garde son en-tête et fait défiler son corps** — `Card fill`, `CardHead`, `CardBody`. Le volet d’édition, la liste des billets et la boîte des messages sont trois cartes de cette forme | Le défilement par colonne : la colonne emmenait la carte entière, si bien que le titre de ce qu’on lisait sortait par le haut, et la gouttière entre deux cartes défilait avec elles |
| D222 | Le pied du rail est une **roue crantée**, et la marque du site reste en tête. Amende D204 | La pastille à l’initiale du compte : posée sous une pastille à l’initiale du site, elle faisait deux cercles lettrés pour une seule personne ; et un avatar promet un compte quand le menu porte aussi la déconnexion |
| D223 | L’**aide d’un champ sort du DSL** : plus de `help` sur `f.*`, plus de phrase sous un contrôle, et le pied d’un champ ne porte plus que sa mesure. Ce qui décrivait un repli ou un effet de bord remonte sous le « ? » ; le `help` d’un **bloc** reste, et paraît là où il sert — la fenêtre « Ajouter une section ». Étend D169, et amende la première clause de D215 | Cesser d’afficher sans retirer l’option : `basalte inventory` annonce ses options dans le `.claude/basalte.md` de chaque dépôt client, et une option que le panel n’honore plus y ferait mentir la doc. Garder les phrases : trente-neuf gloses grises sous les champs mangeaient la colonne, pour expliquer à quelqu’un qui connaît le champ depuis sa deuxième visite — la clarté vient du libellé et de la hiérarchie |
